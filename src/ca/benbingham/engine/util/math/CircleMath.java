package ca.benbingham.engine.util.math;

public class CircleMath {
    public static double volume(double radius) { //TODO needs to be tested
        return (4D / 3D) * Math.PI * Math.pow(radius, 3);
    }
}
